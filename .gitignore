*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
# Every module's build dir, not just the app's — /app/build alone let the
# baselineprofile module's intermediates into a commit.
build/
/app/release
*.apk
*.aab
*.jks
*.keystore
