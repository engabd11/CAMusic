*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
# Kotlin 2.x's own build-state dir (session files, incremental caches). Created by
# any gradle invocation and never checked in.
.kotlin/

# Every module's build dir, not just the app's — /app/build alone let the
# baselineprofile module's intermediates into a commit.
build/
/app/release
*.apk
*.aab
*.jks
*.keystore

# Claude Code's own local tooling state (agent worktrees, local settings) —
# never repo content.
.claude/
